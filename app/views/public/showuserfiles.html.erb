<h1>Files of: <%= User.find(params[:user_id]).name %></h1>
 <%
   User.find(params[:user_id]).annotations.find_all_by_public(true).entries.collect { |annotation|
 %>
  <%= link_to (annotation.name, :controller => "browser", :action=> "seq_id_select", :annotation => annotation.id) %>
  <%= "<br/>" %>
 <%
  }
 %>